import React from 'react';
import ReactDOM from 'react-dom';
import Routes from './Routes';
import './index.scss';

// render our routes with switching as the top level component
//
ReactDOM.render(<Routes />, document.getElementById('root'));
